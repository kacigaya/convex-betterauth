# Created by `bunx convex dev`; public values used by Next.js.
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=
NEXT_PUBLIC_CONVEX_SITE_URL=

# Enable the Google button only when both Google credentials exist on Convex.
NEXT_PUBLIC_GOOGLE_AUTH_ENABLED=false

# Private Better Auth and OAuth secrets belong in the Convex deployment
# environment. Set them with `bunx convex env set`; do not add them here.
